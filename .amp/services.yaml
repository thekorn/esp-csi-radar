services:
  radar-simulator:
    command: env -u LD_LIBRARY_PATH nix develop .#setup --command zig build run-host -- --simulate --bind 0.0.0.0 --port "$PORT"
    health: /api/health
    portal:
      title: ESP CSI room radar
      description: Live four-device room sensing visualization in simulation mode.
