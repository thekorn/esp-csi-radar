services:
  radar-simulator:
    command: bun run host/server.ts --simulate --bind 0.0.0.0 --port "$PORT"
    health: /api/health
    portal:
      title: ESP CSI room radar
      description: Live four-device room sensing visualization in simulation mode.
